<?xml version="1.0" encoding="utf-8" ?>
<the49:BottomSheet
    x:Class="MAUISampleDemo.View.BottomsheetDemoPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:the49="https://schemas.the49.com/dotnet/2023/maui"
    Padding="15,30"
    Background="#3e6374">
    <VerticalStackLayout>
        <Button
            Padding="16,8"
            BackgroundColor="Transparent"
            FontAttributes="Bold"
            Text="Share" />
        <Button
            Padding="16,8"
            BackgroundColor="Transparent"
            FontAttributes="Bold"
            Text="Copy" />
        <Button
            Padding="16,8"
            BackgroundColor="Transparent"
            FontAttributes="Bold"
            Text="Open in browser" />
        <Button
            Padding="16,8"
            BackgroundColor="Transparent"
            FontAttributes="Bold"
            Text="Resize" />
        <Button
            Padding="16,8"
            BackgroundColor="Transparent"
            FontAttributes="Bold"
            Text="Dismiss" />
    </VerticalStackLayout>
</the49:BottomSheet>